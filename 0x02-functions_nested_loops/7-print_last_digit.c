#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - print last digit
 * @num: number
 * Return: answer
 */
int print_last_digit(int num)
{
	putchar((num % 10) + '0');
	putchar((num % 10) + '0');
	return (0);
}
